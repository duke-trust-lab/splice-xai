# Core, conservative floors
numpy>=1.26
Pillow>=9.0.0
matplotlib>=3.7
requests>=2.32.0
python-dotenv>=1.0.0

# Heavy stacks — avoid wrong CUDA wheels on Mac ARM by default
torch>=2.1 ; platform_system != "Darwin" or platform_machine != "arm64"
torchvision>=0.16 ; platform_system != "Darwin" or platform_machine != "arm64"

# Model/tooling layers
ultralytics>=8.3.0
replicate>=1.0.0